---
title: Pagination
sidebar_label: Pagination
hide_table_of_contents: true
hide_title: true
---

import PaginationSample from '../../src/snippets/Pagination';
import ObjectPropList from '../../src/components/ObjectPropList';
import EnumsList from '../../src/components/EnumsList';

<ObjectPropList
  isRequest
  title="Pagination"
  about={(
    <div>
      Core API endpoints that list records are returned in reverse chronological order, with the mostly recently created
      resource showing up first. You may optionally provide the following pagination parameters to limit the
      returned records.
    </div>
  )}
  fields={[
    {
      name: 'from_date',
      type: 'string',
      children: 'ISO 8601 formatted date (YYYY-MM-DD) to filter for records created on and after the date provided.',
    },
    {
      name: 'to_date',
      type: 'string',
      children: 'ISO 8601 formatted date (YYYY-MM-DD) to filter for records created on and before the date provided.',
    },
    {
      name: 'page',
      type: 'string',
      children: 'The number of the page to return.',
    },
    {
      name: 'page_cursor',
      type: 'string',
      children: (
        <p>
          The ID of a resource from which a page should start or end. Mutually exclusive with <code>page</code>.
        </p>
      ),
    },
    {
      name: 'page_limit',
      type: 'string',
      children: 'The number of records to return per page.',
    },
  ]}
  snippetsElement={<PaginationSample />}
  returns={(
    <div>
      Returns a list of the requested resource with the applied pagination.
      Additionally, the following headers are returned with the response:
      <EnumsList
        title="Response pagination headers"
        items={[
          { name: 'Pagination-Page', children: 'The current page.' },
          { name: 'Pagination-Page-Count', children: 'The total number of pages available.' },
          { name: 'Pagination-Page-Limit', children: 'The number of records to return per page.' },
          { name: 'Pagination-Total-Count', children: 'The total number of records matching the request.' },
          { name: 'Pagination-Page-Cursor-Prev', children: 'The previous page cursor.' },
          { name: 'Pagination-Page-Cursor-Next', children: 'The next page cursor.' },
        ]}
      />
    </div>
  )}
/>
