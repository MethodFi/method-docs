---
title: Retrieve a BIN
sidebar_label: Retrieve a BIN
hide_table_of_contents: true
hide_title: true
---

import BinsRetrieve from '../../../../src/snippets/BinsRetrieve';
import ObjectPropList from '../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="Retrieve a BIN"
  about="Returns details for a BIN."
  snippetsElement={<BinsRetrieve />}
  fields={[
    {
      name: 'bin',
      type: 'string',
      children: 'A 4-6 digit number to identify a bin. If only 4 or 5 digits is provided, the response would return partial identifiers.',
      required: true,
    },
  ]}
  returns="Returns details for a BIN."
/>
