---
title: List all accounts
sidebar_label: List all accounts
hide_table_of_contents: true
hide_title: true
---

import AccountsList from "../../../../src/snippets/AccountsList";
import ObjectPropList from '../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="List all accounts"
  about="Returns all the accounts associated with your team, or an empty list if none have been created."
  snippetsElement={<AccountsList />}
  fields={[
    {
      name: 'holder_id',
      type: 'string',
      children: (
        <div>
          The id of the <code>Entity</code> whose accounts to retrieve.
        </div>
      ),
    },
    {
      name: 'type',
      type: 'enum',
      children: (
        <div>
          The type of accounts to retrieve. See <a href="/api/core/accounts/object#type">account types</a>.
        </div>
      ),
    },
    {
      name: 'status',
      type: 'enum',
      children: (
        <div>
          The status of accounts to retrieve. See <a href="/api/core/accounts/object#status">account statuses</a>.
        </div>
      ),
    },
    {
      name: 'liability.mch_id',
      type: 'string',
      children: (
        <div>
          The id of the <code>Merchant</code> to which a liability account is linked.
        </div>
      ),
      ignoreIndent: true,
    },
  ]}
  returns="Returns a list of accounts."
/>
