---
title: Create a micro-deposits verification
sidebar_label: Create a micro-deposits verification
hide_table_of_contents: true
hide_title: true
---

import VerificationCreateMicroDeposits from '../../../../../src/snippets/VerificationCreateMicroDeposits';
import ObjectPropList from '../../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="Create a micro-deposits verification"
  about={(
    <div>
      Verify an ACH account using micro-deposits. Two small amounts (between $0.01 and $0.30) will be
      deposited to the associated ACH account. Once the amounts have been deposited (within 1 - 3 business days),
      the status of the verification will transition from <code>initiated</code> to <code>pending</code>. This is
      when the account holder can <a href="/api/core/accounts/ach-verification/update">verify the two amounts</a>.
    </div>
  )}
  snippetsElement={<VerificationCreateMicroDeposits />}
  fields={[
    {
      name: 'type',
      type: 'enum',
      children: 'The type of verification.',
      enums: [{ name: 'micro_deposits' }],
      required: true,
    },
  ]}
  returns="Returns the verification associated with the id."
/>
