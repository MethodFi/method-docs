---
title: Update a verification
sidebar_label: Update a verification
hide_table_of_contents: true
hide_title: true
---

import VerificationUpdate from '../../../../../src/snippets/VerificationUpdate';
import ObjectPropList from '../../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="Update a verification"
  about={(
    <div>
      This endpoint is to confirm the two amounts (in cents) deposited into the associated
      ACH account. Only verifications of type <code>micro_deposits</code> are allowed to be updated.
      <hr className="dotted-divider" />
      <span className="pill-warning">⚠️ Caution:</span> If multiple unsuccessful attempts have been
      made, the verification will transition into the <code>disabled</code> status. In this case, a
      new ACH account and verification must be created.
    </div>
  )}
  snippetsElement={<VerificationUpdate />}
  fields={[
    {
      name: 'micro_deposits',
      type: 'object',
      children: (
        <div>An object containing update information for a verification of type <code>micro_deposits</code>.</div>
      ),
      required: true,
    },
    {
      name: 'micro_deposits.amounts',
      type: 'number[]',
      children: 'Two numbers to confirm the amounts (in cents) deposited into the associated ACH account.',
      required: true,
    },
  ]}
  returns="Returns the verification associated with the id."
/>
