---
title: List all payments
sidebar_label: List all payments
hide_table_of_contents: true
hide_title: true
---

import PaymentsList from '../../../../src/snippets/PaymentsList';
import ObjectPropList from '../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="List all payments"
  about="Returns all the payments associated with your team, or an empty list if none have been created."
  snippetsElement={<PaymentsList />}
  fields={[
    {
      name: 'type',
      type: 'enum',
      children: 'The type of payments to retrieve.',
      enums: [
        { name: 'standard' },
        { name: 'clearing' },
      ],
    },
    {
      name: 'status',
      type: 'enum',
      children: (
        <div>
          The status of payments to retrieve. See <a href="/api/core/payments/object#status">payment statuses</a>.
        </div>
      ),
    },
    {
      name: 'source',
      type: 'string',
      children: (
        <div>
          The id of the <code>Account</code> that is a payment's source.
        </div>
      ),
    },
    {
      name: 'destination',
      type: 'string',
      children: (
        <div>
          The id of the <code>Account</code> that is a payment's destination.
        </div>
      ),
    },
    {
      name: 'acc_id',
      type: 'string',
      children: (
        <div>
          The id of the <code>Account</code> that is either a payment's source or destination
        </div>
      ),
    },
    {
      name: 'reversal_id',
      type: 'string',
      children: (
        <div>
          The id of the <code>Reversal</code> corresponding to a payment.
        </div>
      ),
    },
    {
      name: 'source_holder_id',
      type: 'string',
      children: (
        <div>
          The id of the <code>Entity</code> who holds the source account in a payment.
        </div>
      ),
    },
    {
      name: 'destination_holder_id',
      type: 'string',
      children: (
        <div>
          The id of the <code>Entity</code> who holds the destination account in a payment.
        </div>
      ),
    },
  ]}
  returns="Returns a list of payment objects."
/>
