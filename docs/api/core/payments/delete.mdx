---
title: Delete a payment
sidebar_label: Delete a payment
hide_table_of_contents: true
hide_title: true
---

import PaymentsDelete from '../../../../src/snippets/PaymentsDelete';
import ObjectPropList from '../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="Delete a payment"
  about={(
    <div>
      Deletes the payment associated with the id.
      <hr className="dotted-divider" />
      <span className="pill-info">💡 Important:</span> Payments can only be deleted when
      in a <code>pending</code> state as they have not been transmitted to our banking networks.
    </div>
  )}
  snippetsElement={<PaymentsDelete />}
  fields={[]}
  returns={<div>Returns the canceled payment with the updated <code>status</code>.</div>}
/>
