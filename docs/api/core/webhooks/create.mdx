---
title: Create a webhook
sidebar_label: Create a webhook
hide_table_of_contents: true
hide_title: true
---

import WebhooksCreate from '../../../../src/snippets/WebhooksCreate';
import ObjectPropList from '../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="Create a webhook"
  about={(
    <div>
      Creates a new webhook for a specific <code>type</code> and <code>url</code>. Your application
      will be notified of updates to the chosen event type.
    </div>
  )}
  snippetsElement={<WebhooksCreate />}
  fields={[
    {
      name: 'type',
      type: 'enum',
      children: (
        <div>
          The event type to be sent to this url. See <a href="/api/core/webhooks/object#type">webhook event types</a>.
        </div>
      ),
      required: true,
    },
    {
      name: 'url',
      type: 'string',
      children: 'The HTTPS URL receiving the webhook event.',
      required: true,
    },
    {
      name: 'auth_token',
      type: 'string,null',
      children: (
        <div>
          Secret token for request validation. Will be sent as a <code>base64</code> encoded string
          in the <code>Authorization</code> header of the webhook.
        </div>
      ),
    },
  ]}
  returns="Returns a webhook object."
/>
