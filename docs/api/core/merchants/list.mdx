---
title: List all merchants
sidebar_label: List all merchants
hide_table_of_contents: true
hide_title: true
---

import ObjectPropList from '../../../../src/components/ObjectPropList';
import MerchantsList from '../../../../src/snippets/MerchantsList';

<ObjectPropList
  isRequest
  title="List all merchants"
  about={(
    <div>
      Returns the most popular <code>liability</code> merchants that are payable using Method's API.
      <hr className="dotted-divider" />
      <span className="pill-info">ℹ️ Info:</span> Only the most popular merchants are returned at
      once in the call to <code>GET /merchants</code>. Use the available filters/query parameters
      to narrow your search and find many more merchants.
    </div>
  )}
  snippetsElement={<MerchantsList />}
  fields={[
    {
      name: 'name',
      type: 'string',
      children: 'Name of the merchant to find.',
    },
    {
      name: 'creditor_name',
      type: 'string',
      children: (
        <div>The name of the creditor as seen in a credit
          report. Ex. <code>JPMCB CARD</code> pertains to a Chase Credit Card.
        </div>
      ),
    },
    {
      name: 'provider_id.plaid',
      type: 'string',
      children: 'Plaid id for the merchant to find.',
      ignoreIndent: true,
    },
    {
      name: 'provider_id.mx',
      type: 'string',
      children: 'MX id for the merchant to find.',
      ignoreIndent: true,
    },
    {
      name: 'provider_id.finicity',
      type: 'string',
      children: 'Finicity id for the merchant to find.',
      ignoreIndent: true,
    },
  ]}
  returns="Returns a list of merchant objects."
/>
