---
title: Update an individual auth session
sidebar_label: Update an individual auth session
hide_table_of_contents: true
hide_title: true
---

import EntitiesUpdateIndividualAuthSession from '../../../../src/snippets/EntitiesUpdateIndividualAuthSession';
import ObjectPropList from '../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  about={(
    <div>
      Update an auth session for an entity to automatically retrieve their
      debt accounts.
      <br/><br/>
      <span className="pill-warning">⚠️ Caution:</span> This endpoint is only
      accessible to entities that have the <code>data:retrieve</code> capability
      in <code>available_capabilities</code>.
    </div>
  )}
  snippetsElement={<EntitiesUpdateIndividualAuthSession />}
  fields={[
    {
      name: 'answers',
      type: 'object[]',
      children: 'A list of the answers for the auth session\'s security questions',
      required: true,
    },
    {
      name: 'answers[].question_id',
      type: 'string',
      children: 'The ID of the security question.',
      required: true,
    },
    {
      name: 'answers[].answer_id',
      type: 'string',
      children: 'The ID of the selected answer corresponding to the security question.',
      required: true,
    },
  ]}
  returns={(
    <div>
      Returns an object with a list of additional security questions (if necessary, otherwise an empty list),
      and the <code>authenticated</code> field indicating whether or not the entity's
      identity was successfully verified.
    </div>
  )}
/>
