---
title: Create element token
sidebar_label: Create element token
hide_table_of_contents: true
hide_title: true
---

import ElementsCreateLinkToken from '../../../../../src/snippets/ElementsCreateLinkToken';
import ElementsCreateLinkToken2 from '../../../../../src/snippets/ElementsCreateLinkToken2';
import ObjectPropList from '../../../../../src/components/ObjectPropList';

<ObjectPropList
  isRequest
  title="Create element token"
  about={(
    <div>
      Creates an element token to launch a Link Element session for a
      specific entity. Element tokens are only valid for 30 minutes after
      they are created, and can only be used once.
      <hr className="dotted-divider" />
      <h4>Example with merchant restriction</h4>
      <p>
        If you have already integrated with a data aggregator (Plaid) to collect your
        user's information. We recommend creating an <code>element_token</code> for a
        specific merchant and mask pair.
      </p>
      <p>
        For example, if your user connected their AMEX credit card ending in 4127 using your own
        Plaid integration, you will create an <code>element_token</code> that will open the Link
        Element directly to a secure AMEX form where the user will confirm their account ending in 4127.
      </p>
      <span className="pill-info">💡 Tip:</span> Use the <a href="/api/core/merchants/list">merchants endpoint</a> to
      find the corresponding <code>mch_id</code> for the identifier used by your
      aggregator. (e.g Plaid's <code>ins_id</code>)
      <hr className="dotted-divider" />
      <h4>Example without merchant restriction</h4>
      <p>
        If you have not integrated with a data aggregator (Plaid), you can
        securely collect your user's information by requesting an <code>element_token</code> without a merchant
        restriction. This will open the Link Element to a search screen for the user to choose which account to link.
      </p>
    </div>
  )}
  snippetsElement={(
    <div>
      <ElementsCreateLinkToken />
      <hr className="dotted-divider" />
      <ElementsCreateLinkToken2 />
    </div>
  )}
  fields={[
    {
      name: 'entity_id',
      type: 'string',
      children: (
        <div>
          The id of the end user (<code>entity</code>) for which the <code>element_token</code> will be created.
        </div>
      ),
      required: true,
    },
    {
      name: 'team_name',
      type: 'string',
      children: 'The name used to refer to your company/app throughout the link flow.',
      required: true,
    },
    {
      name: 'type',
      type: 'enum',
      children: 'The type of element you wish to launch.',
      enums: [{ name: 'link' }],
      required: true,
    },
    {
      name: 'link',
      type: 'object',
      children: 'Link element information.',
      required: true,
    },
    {
      name: 'link.mch_id',
      type: 'string',
      children: (
        <div>
          The <code>mch_id</code> of a merchant to open directly when Link Element is launched.
        </div>
      ),
    },
    {
      name: 'link.mask',
      type: 'string',
      children: 'The last 4 digits of the account number the user will be required to link.',
    },
  ]}
/>
